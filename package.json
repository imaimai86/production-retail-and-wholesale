{
  "name": "production-retail-and-wholesale",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "test": "npm run test --prefix server",
    "generate-spec": "cross-env NODE_ENV=development node server/scripts/generate-openapi-spec.js",
    "start:swagger": "node tools/swaggerServer.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.0.0",
    "express": "^5.1.0",
    "fs-extra": "^11.3.0",
    "husky": "^9.1.7",
    "swagger-ui-express": "^5.0.1"
  }
}
